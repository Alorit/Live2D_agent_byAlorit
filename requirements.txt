# 核心依赖（Python 3.10+，推荐 3.11/3.12/3.13）
PySide6>=6.10
openai>=1.50
numpy>=1.26
jieba>=0.42
pyyaml>=6.0
requests>=2.31
sounddevice>=0.4.6

# 本地 TTS 引擎
sherpa-onnx>=1.12
piper-tts>=1.6

# 可选：在线 TTS 兜底（默认不启用）
edge-tts>=7.0

# MCP 技能客户端（Skills 用）
mcp>=2.0

# 可选：向量长期记忆（需要 PyTorch，体积较大，默认用 BM25 即可）
# sentence-transformers>=3.0
